name: Deploy to Aliyun ACR

on:
  push:
    branches:
      - main

env:
  # 修改为你的阿里云镜像仓库地址
  # 例如: registry.cn-hangzhou.aliyuncs.com/your-namespace/your-repo
  IMAGE_NAME: registry.cn-hangzhou.aliyuncs.com/YOUR_NAMESPACE/gongkao_daka
  REGION: cn-hangzhou

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Login to Aliyun Container Registry
        uses: docker/login-action@v2
        with:
          registry: registry.${{ env.REGION }}.aliyuncs.com
          username: ${{ secrets.ALIYUN_REGISTRY_USER }}
          password: ${{ secrets.ALIYUN_REGISTRY_PASSWORD }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: ${{ env.IMAGE_NAME }}:latest,${{ env.IMAGE_NAME }}:${{ github.sha }}

      # 可选：通过 SSH 连接服务器执行更新命令
      # - name: SSH Remote Commands
      #   uses: appleboy/ssh-action@master
      #   with:
      #     host: ${{ secrets.SERVER_HOST }}
      #     username: ${{ secrets.SERVER_USER }}
      #     key: ${{ secrets.SERVER_SSH_KEY }}
      #     script: |
      #       docker login --username=${{ secrets.ALIYUN_REGISTRY_USER }} --password=${{ secrets.ALIYUN_REGISTRY_PASSWORD }} registry.${{ env.REGION }}.aliyuncs.com
      #       docker pull ${{ env.IMAGE_NAME }}:latest
      #       docker stop gongkao_web || true
      #       docker rm gongkao_web || true
      #       docker run -d --name gongkao_web -p 80:80 --restart always ${{ env.IMAGE_NAME }}:latest
